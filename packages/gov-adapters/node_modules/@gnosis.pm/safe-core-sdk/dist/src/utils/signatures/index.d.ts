import { SafeSignature } from '@gnosis.pm/safe-core-sdk-types';
import { Signer } from 'ethers';
export declare function generatePreValidatedSignature(ownerAddress: string): SafeSignature;
export declare function generateSignature(ethers: any, signer: Signer, hash: string): Promise<SafeSignature>;
