import { Provider } from '@ethersproject/providers';
import { Signer } from 'ethers';
import { GnosisSafe, MultiSend } from '../../typechain';
import { ContractNetworksConfig } from '../configuration/contracts';
declare class ContractManager {
    #private;
    static create(ethers: any, safeAddress: string, chainId: number, currentProviderOrSigner: Provider | Signer, provider: Provider, contractNetworks?: ContractNetworksConfig): Promise<ContractManager>;
    init(ethers: any, safeAddress: string, chainId: number, currentProviderOrSigner: Provider | Signer, provider: Provider, contractNetworks?: ContractNetworksConfig): Promise<void>;
    get safeContract(): GnosisSafe;
    get multiSendContract(): MultiSend;
}
export default ContractManager;
