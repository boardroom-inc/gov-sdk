import { OperationType, SafeTransaction } from '@gnosis.pm/safe-core-sdk-types';
import { GnosisSafe } from '../../../typechain';
export declare function estimateTxGas(safeContract: GnosisSafe, to: string, valueInWei: string, data: string, operation: OperationType): Promise<number>;
export declare function estimateGasForTransactionExecution(safeContract: GnosisSafe, from: string, tx: SafeTransaction): Promise<number>;
